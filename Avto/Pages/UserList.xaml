<Page x:Class="Avto.Pages.UserList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Avto.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="UserList">

    <Grid>
        <DataGrid x:Name="DgListUsers" AutoGenerateColumns="False" Loaded="DgListUsers_Loaded" Padding="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№п/п"
                                        Binding="{Binding IdUser}" />
                <DataGridTextColumn Header="Логин"
                                    Binding="{Binding LoginUser}"/>
                <DataGridTextColumn Header="Пароль"
                                    Binding="{Binding PasswordUser}"/>
                <DataGridTextColumn Header="Роль"
                                    Binding="{Binding NameRole}"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
