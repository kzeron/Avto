<Page x:Class="Avto.Pages.TarifList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Avto.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TarifList">

    <Grid>
        <DataGrid x:Name="DgListTariffsAdmin" AutoGenerateColumns="False" Loaded="DgListTariffsAdmin_Loaded" Padding="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№п/п"
                                      Binding="{Binding IdTariff}" />
                <DataGridTextColumn Header="Тариф"
                                  Binding="{Binding NameTariff}"/>
                <DataGridTextColumn Header="Пароль"
                                  Binding="{Binding PriceTariff}"/>


            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
